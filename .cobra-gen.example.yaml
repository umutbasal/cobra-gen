cmd:
- --region
- --output
- --query
- --dryrun
- --debug
- --version
- --profile
- --endpoint-url
- plugin-test-recursive:
  - ctrl:
    - delete:
      - +resource
      - something:
        - --force
  - manage:
    - get:
      - --namespace
- s3:
  - --recursive
  - --exclude
  - --include
  - ls:
    - +bucket
    - --no-paginate
    - --page-size
  - cp:
    - +source
    - +destination
  - mv:
    - +source
    - +destination
  - rm
  - sync
  - cpp:
    - --sasd
- ec2:
  - run-instances:
    - --instance-type
    - --key-name
    - --image-id
  - terminate-instances
  - describe-instances
  - describe-images
  - describe-volumes
  - describe-snapshots
  - create-tags
  - delete-tags
